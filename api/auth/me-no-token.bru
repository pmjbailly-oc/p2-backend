meta {
  name: me no token
  type: http
  seq: 4
}

get {
  url: http://localhost:8080/api/me
  body: none
  auth: none
}

script:post-response {
  test("response status is 401", () => {
    expect(res.status).to.equal(401);
  });
}

settings {
  encodeUrl: true
}